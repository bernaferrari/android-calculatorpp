package org.solovyev.android.calculator.ui

import androidx.compose.ui.text.font.FontFamily

/**
 * Android implementation uses default (will be replaced with custom font later if needed)
 * For now, using system default for consistent KMP build.
 */
actual val CalculatorFontFamily: FontFamily = FontFamily.Default
